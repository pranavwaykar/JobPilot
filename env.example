# SMTP settings (recommended: use Gmail App Password or a dedicated SMTP provider)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Sender identity
FROM_EMAIL=pawarshubham1295@gmail.com
FROM_NAME=Shubham Pawar

# Attachment path (put your PDF here)
RESUME_PATH=assets/Shubham_Pawar_3Yr.pdf

# Scheduling
# Runs once per day at 09:00 (cron syntax: min hour day month weekday)
SCHEDULE_CRON=0 9 * * *
TIMEZONE=Asia/Kolkata

# Sending behavior
DELAY_MS_BETWEEN_EMAILS=1500
DRY_RUN=false

# Email content overrides (optional)
SUBJECT=Application for MERN Stack Developer Role — Immediate Joiner | 3 Yrs Experience

# UI Login (optional but recommended)
# If set, the UI (and all API routes) will require login.
UI_AUTH_USER=admin
UI_AUTH_PASS=


